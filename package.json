{
  "name": "ts-avanzado",
  "version": "1.0.0",
  "description": "## Instalando TypeScript con Node En la terminal, ejecutemos npm `init -y` dentro de la ruta de nuestro proyecto para inicializar Node con una configuración por defecto.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "John Serrano",
  "license": "MIT",
  "devDependencies": {
    "@faker-js/faker": "^8.0.2",
    "ts-node": "10.9.1",
    "typescript": "5.1.6"
  },
  "dependencies": {
    "axios": "1.5.0"
  }
}
